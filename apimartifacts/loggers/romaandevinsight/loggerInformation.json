{
  "properties": {
    "loggerType": "applicationInsights",
    "credentials": {
      "instrumentationKey": "${{secrets.LOGGER_CREDENTIALS}}"
    },
    "isBuffered": true,
    "resourceId": "{{RESOURCE}}"
  }
}
