{
  "properties": {
    "loggerType": "applicationInsights",
    "credentials": {
      "instrumentationKey": "{{LOGGER_CREDENTIALS}}"
    },
    "isBuffered": true,
    "resourceId": "{{RESOURCE}}"
  }
}
